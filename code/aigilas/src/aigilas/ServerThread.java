package aigilas;public class ServerThread implements Runnable {	@Override	public void run() {		try {			System.out.println("Server loop");			// Server.Get();			// Thread.sleep(50);			// while (Server.Get().IsOnlyInstance()) {			// Server.Get().Update();			// Thread.sleep(50);			// }		}		catch (Exception e) {			System.out.println(e.getMessage());		}		System.out.println("Finished being a server");	}}